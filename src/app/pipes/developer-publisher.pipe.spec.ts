import { DeveloperPublisherPipe } from './developer-publisher.pipe';

describe('DeveloperPublisherPipe', () => {
  it('create an instance', () => {
    const pipe = new DeveloperPublisherPipe();
    expect(pipe).toBeTruthy();
  });
});
